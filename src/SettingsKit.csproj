<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <RootNamespace>AluminiumTech.DevKit.SettingsKit</RootNamespace>
        <GeneratePackageOnBuild>true</GeneratePackageOnBuild>
        <Title>SettingsKit</Title>
        <Authors>AluminiumTech</Authors>
        <Copyright>Copyright (c) AluminiumTech 2021</Copyright>
        <RepositoryUrl>https://gitlab.com/aluminiumtechdevkit/SettingsKit/</RepositoryUrl>
        <RepositoryType>Git</RepositoryType>
        <PackageReleaseNotes>### Changes
* Start migration away from ``Preference&lt;TKey, TValue&gt;`` - All previous functionality has been moved to ``KeyValueDataStore`` and it nows inherits ``KeyValueDataStore`` - This is now deprecated and will likely be removed in 4.x
* Removed some unneeded properties and variables from ``KeyValueDataStore&lt;Tkey, TValue&gt;`` 
* Added new ``ValueRangeDataStore&lt;TKey, TValue&gt;`` to be designed to be more appropriate for numerical Settings Values where a minimum and maximum value is provided or expected - This class inherits ``KeyValueDataStore&lt;TKey, TValue&gt;``.</PackageReleaseNotes>
        <PackageRequireLicenseAcceptance>true</PackageRequireLicenseAcceptance>
      <AssemblyName>SettingsKit</AssemblyName>
        <Description>A library to make managing JSON based Settings files easier for .NET .</Description>
        <Product>SettingsKit</Product>
        <LangVersion>latest</LangVersion>
        <TargetFrameworks>net5.0;netcoreapp3.1;netstandard2.0</TargetFrameworks>
        <PackageReadmeFile>README.md</PackageReadmeFile>
        <PackageLicenseFile>LICENSE.md</PackageLicenseFile>
        <AssemblyVersion>3.1.0.0</AssemblyVersion>
        <FileVersion>3.1.0.0</FileVersion>
        <Version>3.1.0</Version>
        <PackageVersion>3.1.0</PackageVersion>
    </PropertyGroup>

    <ItemGroup>
      <PackageReference Include="Newtonsoft.Json" Version="13.0.1" />
    </ItemGroup>

    <ItemGroup>
        <None Include="..\README.md">
            <Pack>True</Pack>
            <CopyToOutputDirectory>Always</CopyToOutputDirectory>
            <PackagePath></PackagePath>
        </None>
    </ItemGroup>

    <ItemGroup>
        <None Include="..\LICENSE.md">
            <Pack>True</Pack>
            <CopyToOutputDirectory>Always</CopyToOutputDirectory>
            <PackagePath></PackagePath>
        </None>
    </ItemGroup>

</Project>
